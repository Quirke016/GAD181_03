using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SS_EventControl : MonoBehaviour
{
    /*[Range(1, 4)]
    public int numberOfPlayers;
    GameObject[] players;
    public GameObject playerPrefab;
    SS_PlayerControl[] playerControls;
    float screenSize;
    public GameObject simonObject;
    int[] playerRowPoint;
    int[] playerPoints;
    // Start is called before the first frame update
    void Start()
    {
        patternSoftWere = simonObject.GetComponent<SS_PatternGen>();

        simonPattern = patternSoftWere.simonPattern;

        symbolContral = playerSymbol.GetComponent<SS_SymbolControl>();

        newRound = true;
    }

    void EnemyRound()
    {
        patternSoftWere.StartEnemyRound();
    }

    void PlayerRound() // if time do this one
    {
        playerGuesss.Clear();
    }

    void SpawnPlayers()
    {

    }



  *//*  // Start is called before the first frame update
    void Start()
    {
        
    }*//*

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
